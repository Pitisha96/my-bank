server:
  port: 8081

spring:
  application:
    name: account-service

  datasource:
    url: ${POSTGRES_URL}
    username: ${POSTGRES_USERNAME}
    password: ${POSTGRES_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        default_schema: my_bank
        type:
          json_format_mapper: com.pitisha.project.accountservice.domain.entity.mapper.CustomJsonFormatMapper

  liquibase:
    enabled: true
    contexts: dev
    default-schema: my_bank
    liquibase-schema: my_bank

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI}